# 桌面版微信自动化依赖
# 仅支持 Windows 平台运行

pywinauto>=0.6.8      # UI 自动化核心（基于 UIA / Win32 后端）
pywin32>=305          # Windows API、剪贴板、文件复制支持
pyperclip>=1.8.2      # 跨平台文本剪贴板读写
Pillow>=9.0.0         # 图片处理 / 截图（可选，用于图片写入剪贴板）
