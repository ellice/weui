# 桌面版微信自动化依赖
# 仅支持 Windows 平台

pywinauto>=0.6.8        # UIAutomation / Win32 后端，控件定位与键鼠模拟
pyperclip>=1.8.2        # 文本剪贴板读写
pillow>=9.0.0           # 图片处理（可选，用于图片转剪贴板）
pywin32>=305            # 文件/图片写入剪贴板（CF_HDROP / CF_DIB）
